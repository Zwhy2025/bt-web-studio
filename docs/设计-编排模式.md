# 编排模式（Composer Mode）设计文档

编排模式是 BT Web Studio 三模式之一，负责**设计阶段**：为算法工程师、系统集成开发者等提供所见即所得的行为树编排工作台，支持从节点库拖拽、连线、配置属性与端口，最终导入/导出为 BehaviorTree.CPP 兼容的 XML。本文档为产品设计与实现对齐的基准。

## 1. 业务目标与范围

### 1.1 目标
提供所见即所得的行为树编排工作台，支持节点库拖拽、连线、属性与端口配置，以及 XML 导入/导出，与 BehaviorTree.CPP 运行时无缝对接。

### 1.2 范围
- 节点库浏览与检索（按类别 Action/Condition/Control/Decorator/SubTree）
- 画布编辑（拖拽创建、吸附、对齐、缩放平移、框选、连线/断开）
- 节点属性与端口配置（详见 [设计-属性面板](设计-属性面板.md)）
- 数据导入/导出（XML ⇆ ReactFlow 节点/边结构）
- 状态管理与撤销重做、基础校验

**不在本阶段范围**：实时调试、断点、运行态高亮（由调试/回放模式处理）。

### 1.3 角色与场景
- **关卡/剧情设计师**：拖拽流程、条件与行为节点，串接逻辑，定义参数与输入输出，导出 XML 给运行时引擎
- **工程师**：导入运行工程导出的 XML，校对并调整节点属性与端口，保持与代码侧节点定义一致

## 2. 用户流程（高层）
1. 从左侧节点库检索并拖拽节点到画布
2. 选择节点，在右侧属性面板配置属性与端口（默认 1 入/1 出，可增删改）
3. 将上游节点输出端口连接到下游节点输入端口
4. 视图控制（吸附、对齐、缩放、适配视图）
5. 导出为 XML 或从 XML 导入继续编辑

## 3. 核心概念与数据模型

### 3.1 信息结构
- **Node（节点）**：行为树构成单元，含分类、显示名、描述、属性、端口（inputs/outputs）
- **Edge（边）**：连接节点端口的有向连线，代表父子/数据/控制流关系（当前为父子控制流）
- **Port（端口）**：可连接点，支持 side: top/bottom/left/right，多端口按侧均匀分布
- **Category（类别）**：Action/Condition/Control/Decorator/SubTree

**Glossary**：Composer Mode（编排模式）、React Flow（画布基础库）、XML（BehaviorTree.CPP 数据交换格式）

### 3.2 数据模型（前端）
```ts
interface BehaviorTreeNodeData {
  id: string
  name: string
  category: string // action | condition | control | decorator | subtree
  description?: string
  properties?: Record<string, any>
  inputs?: Array<{ id: string; label?: string; side?: 'top'|'bottom'|'left'|'right' }>
  outputs?: Array<{ id: string; label?: string; side?: 'top'|'bottom'|'left'|'right' }>
}
```
- 端口默认值：未设置则 `inputs:[{id:'in', side:'top'}], outputs:[{id:'out', side:'bottom'}]`
- 边：React Flow `Edge`，类型 `smoothstep`，收尾带箭头

## 4. 交互规则

### 4.1 画布
- **空白处左键/中键拖拽**：平移画布
- **Shift + 左键拖拽**：框选多个节点
- **滚轮**：缩放；**滚轮 + 拖拽**：平移（panOnScroll）
- **右键**：上下文菜单（全选、复制、删除、自动布局、网格吸附）

### 4.2 拖拽与放置
- DataTransfer 类型 `application/reactflow`，载荷含节点元数据（id/name/category/...）
- 放置时按吸附设置对齐到网格（默认 20px）

### 4.3 选择与多选
- 点击/框选；`Ctrl/Cmd` 追加选择；Delete/Backspace 删除选中

### 4.4 连线
- 从 source（输出端口）到 target（输入端口）
- 禁止产生环（循环检查参考 bt-canvas 实现）
- 禁止连到 root（root 只能作为 source）

### 4.5 吸附与样式
- 吸附到网格：默认开启，20px
- 节点样式：明暗主题适配；分类色块（蓝/绿/紫/橙/靛）；编排模式默认 `idle`
- 编排模式隐藏断点/禁用/设置等调试类悬浮控件

## 5. 与属性面板的衔接

属性面板的详细设计（五区划分、Root 规则、端口配置、模型驱动、约束与校验）见 [设计-属性面板](设计-属性面板.md)。编排模式仅负责画布侧交互；节点选中后，属性配置在右侧属性面板完成。

## 6. 导入/导出（XML 对齐）
- **导入**：`parseXML(xml)` → nodes/edges，兼容 BehaviorTree.CPP 常见节点类型
- **导出**：`generateXML(nodes, edges)` → XML；含根节点校验、孤立节点检查、重复根提示
- 节点属性与端口的映射在生成层定义（见属性面板/端口编辑器映射策略）

## 7. 校验与错误处理
- **结构校验**：无根/多根、孤立节点、潜在环
- **端口校验**：端口 id 唯一、端口方向与连接一致性（source/target）
- **提示**：Toast/Panel 标注，轻量不打断操作

## 8. 与设计文档体系的衔接

编排模式从 [设计-三模式架构](设计-三模式架构.md) 继承 ORCHESTRATION 职责。属性面板、顶部工具栏的详细规范见 [设计-属性面板](设计-属性面板.md)、[设计-顶部工具栏](设计-顶部工具栏.md)。推荐阅读顺序：概要设计 → 三模式架构 → 本文档 → 属性面板。

## 9. 技术架构与代码参考

- **技术栈**：React + React Flow、Zustand、i18next、Tailwind CSS
- **画布**：`src/components/layout/composer-canvas.tsx`
- **节点**：`src/components/nodes/behavior-tree-node.tsx`、`control-sequence-node.tsx`
- **节点库**：`src/components/layout/node-library-panel.tsx`
- **Store**：`src/core/store/behavior-tree-store.ts`、`composerModeState.ts`
- **XML**：`src/core/bt/xml-utils.ts`、`global-xml-processor.ts`
- **入口**：`src/app.tsx` → `ModeAwareLayout` → `composer-layout`

## 10. 可视样式规范
- 节点卡片：宽度 120–220px，圆角 `lg`，边框 2px；头部 `bg-accent/10`，左侧图标方块使用类别色
- 端口：小圆点 `w-3 h-3`，`border-border bg-background`
- 画布背景：Dots，间距 20px，颜色 `--muted-foreground/15%`

## 11. 路线图
- **v1**：端口编辑器、选择框内批量操作与对齐工具条、XML 属性映射配置化
- **v2**：SubTree 引用与折叠/展开、约束校验（必连端口、端口类型、类型兼容矩阵）
- **v3**：与调试/回放模式联动（从运行事件回写状态到节点）

## 12. TODO：功能实现状态

以下按「已有」「部分实现」「未实现」分类。实现时需对接 `treeState.actions`（nodes/edges/importData/deleteNode）与 `composerModeState.composerActions`。

### 12.1 已有功能

| 功能 | 位置 | 说明 |
|------|------|------|
| 节点库浏览与检索 | `node-library-panel.tsx` | 按类别筛选、搜索、拖拽到画布 |
| 拖拽创建节点 | `composer-canvas.tsx` onDrop | 支持 application/reactflow、json、text/plain |
| 画布平移/缩放/框选 | `composer-canvas.tsx` | 空白拖拽=平移，Shift+拖拽=框选 |
| 右键菜单 | `composer-canvas.tsx` ContextMenu | 全选、复制、删除、自动布局、网格吸附 |
| XML 导入/导出 | `xml-dialog.tsx` | parseXMLUnified、generateXML |
| 连线 | `composer-canvas.tsx` onConnect | 禁止连到 root |
| 吸附、属性面板、端口配置、Root 规则、节点校验、自动布局、全选 | 见各组件 | — |

### 12.2 部分实现（占位）

| 功能 | 现状 | 实现要点 |
|------|------|----------|
| 删除节点 | `deleteSelectedNodes` 仅 console.log | 调用 `treeState.actions.deleteNode` 循环删除，`importData` 同步 |
| 复制/剪切/粘贴 | clipboard 未写入 | 序列化 nodes+edges 到 clipboard；粘贴时生成新 id、偏移 position、`importData` |
| 撤销/重做 | 只改 historyIndex | `addToHistory` 保存 `{ nodes, edges }` 快照；undo 时 `importData` 恢复 |
| 选择框计算 | `selectedNodeIds` 恒为 [] | `updateSelectionBox` 中做矩形与节点 position 碰撞检测 |
| 对齐工具 | 仅 console.log | 计算 bounds，批量 `updateNode` 更新 position |

### 12.3 未实现

| 功能 | 实现要点 |
|------|----------|
| 连线循环检测 | onConnect 中 DFS/BFS 检测环，拒绝并 toast |
| 对齐参考线 | 参考 `bt-canvas` 的 `AlignmentGuides` |
| 结构校验面板 | 新建 validator，在属性面板或独立 Tab 展示 |
| XML 属性映射配置化 | NodeModelRegistry + propertiesToXml/xmlToProperties |
| SubTree 引用与折叠 | 画布支持 SubTree 节点、折叠展开 UI |
| 模型驱动 portsSchema | Node Registry 按 schema 渲染端口 |
| 双击节点聚焦属性面板 | 画布 `onNodeDoubleClick` 调用 `openNodeSettings(nodeId)` |
